Good. Implement this, but keep the naming and story very clear.

Use:

02_candidates/
  derive_candidates.py
  candidate_nodes.csv
  candidate_edges.csv
  candidate_conditions.csv

The candidate files are evidence-only outputs.

Important:
- derive_candidates.py must NOT read or write 03_schema/
- derive_candidates.py must NOT overwrite the final schema
- derive_candidates.py must NOT generate account_graph.yaml or edge_policy.yaml
- candidate_nodes.csv / candidate_edges.csv / candidate_conditions.csv are not the final schema
- final schema remains curated in 03_schema/

Update README.md to say:

01_source_of_truth/ contains Method3 extracted evidence.
02_candidates/ contains deterministic candidate graph structures derived from that evidence.
03_schema/ contains the curated final graph schema contract.
05_validators/ contains deterministic validators for the final schema.
06_reports/ contains generated validation outputs.

Update run_all_checks.sh so the first step is labelled:

0. Deriving evidence-based graph candidates

not:

0. Validating candidates

Then run:

cd consumption_inventory_graph
./run_all_checks.sh

Return:
1. final tree
2. files created/changed
3. candidate row counts
4. PASS/FAIL summary
5. confirmation 03_schema/ was not modified
