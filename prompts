We need to investigate real canonical gaps before changing LinkML.

Do not change any YAML files yet.
Do not update LinkML yet.
Do not generate ETL code yet.
Do not redesign the model.

Use only:
- Method3 code
- extraction/method3_extraction_report.md
- extraction/method3_sources.csv
- extraction/method3_joins.csv
- extraction/method3_literals.csv
- extraction/method3_output_columns.csv
- extraction/method3_audit_fields.csv
- model_linkml/reference/manual_visual_schema.json
- model_linkml/reference/visual_schema_mapping.md
- model_linkml/canonical_contract_table.md

Create this file:

model_linkml/reference/gap_investigation_report.md

Investigate these gaps:

1. CommDataIQAsset
   Question: How does Method3 connect database/table access records to CommDataIQ catalogue/assets?
   Check whether the join is by:
   - table only
   - database only
   - database + table
   - db_table
   - cdiq_name / cdiq_id
   - cdiq_layer / cdiq_data_platform
   - normalised or uppercased fields
   - fuzzy or exact matching

2. DirectoryPerson
   Question: How does Method3 resolve real people/owners/managers?
   Check:
   - AD/AAD join
   - Workday/Work Location join
   - owner/contact email join
   - manager fallback
   - person ID/email/name fields
   - whether DirectoryPerson should be separate from Identity

3. XCOMSystem
   Question: How does Method3 represent XCOM/downstream batch systems?
   Check:
   - exact source file/table
   - join key
   - xcom_remote or equivalent column
   - whether it applies only to Batch accounts
   - whether it is distinct from ApplicationService

4. ConfigurationItem → ApplicationService
   Question: How does Method3 connect CI/CMDB records to application/service?
   Check:
   - CI key columns
   - CMDB app/service columns
   - app/service identifier
   - app/service name
   - whether CI-to-application edge is exact, conditional, or missing

For each gap, return a table with:

| gap | method3_function | source_files_tables | join_keys | output_columns | match_type_exact_or_conditional | stable_id_available | proposed_canonical_class | proposed_edges | provenance_columns | confidence | implementation_decision |

Implementation decision must be one of:
- add_now
- add_conditional_now
- visual_mapping_only
- backlog
- do_not_add

Rules:
- If evidence is not explicit, write "not found".
- Do not infer silently.
- Use line numbers or extraction file references where available.
- At the end, give a short "Recommended minimal LinkML changes" section.
