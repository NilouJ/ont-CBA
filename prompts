Yes, apply those fixes.

Rules:
1. For source_files_or_tables_used, join_keys_used, and final_output_columns_produced:
   - replace vague wording with explicit values from the extraction files where available
   - if the extraction does not explicitly support the value, write "not found"
   - do not infer silently

2. For Unknown / Unknown rows:
   - mark them clearly as derived_post_method3 = true
   - explain that Unknown is not a native Method3 output class
   - keep unresolved/ambiguous reason in unresolved_conflict_handling

3. Do not redesign the taxonomy.
4. Do not generate LinkML yet.
5. Do not generate ETL code yet.

After applying the fixes, return:
- the updated canonical_contract_table.md
- a short list of exact changes made
