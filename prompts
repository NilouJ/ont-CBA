Keep business_validation_rules.yaml simple for now.

Do not redesign the business rules.
Do not implement deep business validation.
Do not check freshness, M1/M2 precedence, manager chain, or source authority yet.

For now I only want schema/shape validation.

Update check_consistency.py to perform only these checks:

C1. account classes are valid enum values
C2. account subtypes are valid enum values
C3. every edge source/target class in edge_policy.yaml exists in account_graph.yaml
C4. every classification rule output uses valid account_class/account_subtype
C5. every source_mapping output column exists as a slot in account_graph.yaml
C6. every materialised edge in edge_policy.yaml declares provenance/evidence fields
C7. no duplicate class names, slot names, or edge labels
C8. any edge label referenced in business_validation_rules.yaml must exist in edge_policy.yaml

Do not validate detailed business logic yet.

After updating the checker, run:
python3 model_linkml/check_consistency.py
.venv/bin/linkml-lint model_linkml/account_graph.yaml
.venv/bin/linkml-validate --schema model_linkml/account_graph.yaml --target-class Account model_linkml/examples/valid/account.valid.yaml
.venv/bin/gen-json-schema model_linkml/account_graph.yaml > model_linkml/generated/jsonschema/account_graph.schema.json

Return only:
- checker result
- lint error count
- validation result
- JSON schema generated yes/no
