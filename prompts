Good. This canonical contract table is approved as the basis for implementation.

Now generate the five YAML files from this table.

Important constraints:
1. canonical_account_class and canonical_account_subtype are the graph taxonomy.
2. method3_account_type and method3_account_subtype must be preserved only as metadata fields.
3. Unknown/Unknown is not native Method3 output; mark it as derived_post_method3.
4. Add classification_confidence and classification_evidence fields because some reclassification depends only on AD match/null.
5. Do not create separate graph node classes for User, Application, Batch, Human-Teradata, Human-Omnia, SU-reclassified, ACOE-service-account, GDW-Batch.
6. Use one canonical Account class with account_class and account_subtype fields.
7. Keep source/evidence/provenance fields where available.
8. Do not generate ETL code yet.

Create exactly these files:

1. modelLinkml/account_graph.yaml
   - LinkML schema only
   - classes, slots, enums
   - canonical Account class
   - classes: Account, Identity, BusinessUnit, Database, Table, ConfigurationItem, ApplicationService, SandpitRecord if supported
   - edge classes based on the contract table
   - include metadata slots:
     method3_account_type
     method3_account_subtype
     classification_reason
     classification_confidence
     classification_evidence
     source_file
     sheet
     source_record_id
     evidence_id
     resolution_status

2. modelLinkml/source_mapping.yaml
   - map Method3 final output columns to canonical graph fields

3. modelLinkml/classification_rules.yaml
   - rules mapping Method3 output classes/subtypes to canonical account_class/account_subtype
   - include source condition, confidence, and caveat

4. modelLinkml/edge_policy.yaml
   - edge_label, source_vertex, target_vertex, materialise_when, evidence_required

5. modelLinkml/business_validation_rules.yaml
   - checks for:
     allowed account_class/account_subtype
     Unknown only derived downstream
     edge endpoints exist
     materialised edges have evidence
     low/medium confidence cases go to audit
     unresolved/conflicting records go to audit

Output only the YAML contents.
