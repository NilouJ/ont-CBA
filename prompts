Clean the human-readable comments in the clean package.

Do not change schema logic.
Do not change edge logic.
Do not change class names, property names, enum values, edge labels, source nodes, or target nodes.
Do not modify original model_linkml files.
Only update files inside consumption_inventory_graph/.

Required changes:

1. Add the curated contract table into the clean package

Copy:

model_linkml/canonical_contract_table.md

to:

consumption_inventory_graph/schema/canonical_contract_table.md

This file is the curated design contract behind the final schema.

2. Remove rubbish / confusing comments from YAML files

In these clean package files only:

consumption_inventory_graph/schema/account_graph.yaml
consumption_inventory_graph/schema/edge_policy.yaml
consumption_inventory_graph/schema/business_validation_rules.yaml
consumption_inventory_graph/mapping/source_mapping.yaml
consumption_inventory_graph/mapping/classification_rules.yaml

Remove or rewrite comments that say things like:

- source of truth: model_linkml/canonical_contract_table.md
- No ETL code
- No generated schema artefacts yet
- derived from Method3 output
- any old path pointing to model_linkml/
- any messy working-note comments

3. Use clean wording

For extractor evidence files / mapping files, use:

"Source of truth: extractor outputs under extractor/outputs/."

For schema files, use:

"Schema namespace: cba.com"

For curated schema design comments, use:

"Curated schema contract: schema/canonical_contract_table.md"

4. Update schema IDs / example URLs only if they are human-readable metadata and safe to update

Where current YAML has example placeholder IDs like:

https://example.org/account-graph
https://example.org/edge-policy

replace with CBA-style metadata such as:

https://cba.com/consumption-inventory/account-graph
https://cba.com/consumption-inventory/edge-policy
https://cba.com/consumption-inventory/business-validation-rules

Do not change technical imports/prefixes that are required by the schema tooling.

Important:
If there is a required technical prefix like:

linkml: https://w3id.org/linkml/

leave it unchanged.

5. README wording

Update README to say:

"The extractor outputs are the deterministic evidence source. The canonical contract table is the curated design contract used to define the final graph schema, edge policy, and business validation rules. The schema files use a CBA-style namespace for package metadata."

Do not claim:
- edge labels are extracted directly from Method3
- the final ontology is automatically generated
- Claude generated the schema
- LinkML is the external-facing method

6. Rerun checks

Run:

cd consumption_inventory_graph
./run_checks.sh

Return:
1. files changed
2. exact comments/metadata updated
3. confirmation schema logic did not change
4. confirmation original model_linkml files were not modified
5. PASS/FAIL summary
6. final tree
