Now write a small Python consistency checker for these five YAML files.

It must check:
1. account classes are only Human, NonHuman, Unknown
2. account subtypes are only Human, ServiceAccount, Batch, Unknown
3. all edge source/target classes exist in account_graph.yaml
4. all classification rule outputs use valid account_class/account_subtype values
5. all source_mapping fields exist as slots in account_graph.yaml or are marked method3_metadata
6. every materialised edge requires evidence/provenance fields
7. Unknown/Unknown rows have derived_post_method3 = true

Do not redesign the YAMLs.
Return the script and how to run it.
