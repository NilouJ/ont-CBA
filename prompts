Stop. The package is too complicated.

I want the clean package reduced to only three core parts:

1. extractor/
2. mapping/
3. schema/

Do not modify original model_linkml files.
Do not change schema logic.
Do not change edge logic.
Do not redesign the model.

Delete/remove from the clean package:
- reports/
- evidence/
- schema_contract/
- validators/ unless needed for one simple check
- 03_schema/
- numbered folders
- candidate folders
- visual artefacts
- gap reports
- logs
- extra investigation files

Final structure must be:

consumption_inventory_graph/
  README.md
  run_checks.sh

  extractor/
    extract_method3_evidence.py
    outputs/
      method3_output_columns.csv
      method3_joins.csv
      method3_sources.csv
      method3_audit_fields.csv
      method3_literals.csv

  mapping/
    source_mapping.yaml
    classification_rules.yaml

  schema/
    account_graph.yaml
    edge_policy.yaml
    business_validation_rules.yaml
    account_graph.schema.json

Optional only if already working and simple:
  validation/
    validate_schema_freeze.py
    check_consistency.py
    validation_result.json

The story must be:

Method3 extractor evidence
  -> deterministic evidence extraction
  -> deterministic source-to-schema mapping
  -> curated graph schema contract
  -> generated JSON schema

Important wording:
- Extractor output is deterministic.
- Source mapping is deterministic.
- Schema/edge design is curated and evidence-informed.
- Do not claim edge labels are automatically extracted from Method3.
- Do not claim final ontology is auto-generated.

Update README.md to explain only this simple story.

Create run_checks.sh that only runs:
1. python3 extractor/extract_method3_evidence.py
2. python3 validation/validate_schema_freeze.py if validation folder is kept
3. python3 validation/check_consistency.py if validation folder is kept

If validation folder is removed, run_checks.sh should only validate that required files exist and extractor outputs are readable.

Return:
1. final tree
2. removed folders/files
3. files kept
4. one-paragraph explanation of the package
5. confirmation original model_linkml files were not modified
6. confirmation schema files were not changed semantically
