Pause and check Identity/AAD to DirectoryPerson.

I think the schema is missing the person identity resolution path.

Do not redesign the model.
Do not add HumanAccount/ServiceAccount/BatchAccount as physical classes.
Do not add REPORTS_TO.

Check Method3 output and schema fields for identity/person evidence:
- ad_upn
- ad_display_name
- email
- lan_id
- sa_contactemail
- wd_cmdb_owned_by_email
- any Workday/person email/name fields

If evidence exists, add this conditional/derived edge:

RESOLVES_TO_PERSON:
  source: Identity
  target: DirectoryPerson
  semantic_layer: derived_reference
  materialisation_status: conditional
  assertion_status: derived
  evidence_class: derived_from_ad_workday_or_owner_email
  provenance_required: true

Materialise only when:
- an Identity identifier exists, such as ad_upn/email/lan_id
AND
- a DirectoryPerson identifier exists, such as email/person_id/display_name/contact email

Do not claim Method3 directly outputs this as a raw edge.
It is a derived graph edge from identity/person fields.

Also update the visual/visjs model so the path is visible:

Account -> HAS_IDENTITY -> Identity -> RESOLVES_TO_PERSON -> DirectoryPerson -> BELONGS_TO -> BusinessUnit

Keep Account -> HAS_OWNER -> DirectoryPerson as well, because owner/contact resolution is a separate path.

After changes, rerun:
python3 model_linkml/check_consistency.py
.venv/bin/linkml-lint model_linkml/account_graph.yaml
.venv/bin/linkml-validate --schema model_linkml/account_graph.yaml --target-class Account model_linkml/examples/valid/account.valid.yaml
python3 model_linkml/scripts/convert_schema_to_visjs.py --validate-only

Return only:
- whether Method3 has enough evidence
- edge added or not
- files changed
- checker result
- lint error count
- validation result
- visjs converter result
