Pause and follow this exactly.

Do not redesign anything.
Do not change schema logic.
Do not change edge logic.
Do not modify the accepted final schema design.
Do not modify original model_linkml files.

We are only improving the clean package:

consumption_inventory_graph/

Goal:
Make the package tell this simple story:

1. Method3 extractor package is the source of truth.
2. Truth tables are extracted deterministically from Method3 evidence.
3. Candidate nodes/edges/conditions are derived deterministically from those truth tables.
4. The final graph schema is a curated contract refined from the existing base schema using those evidence-backed candidates.
5. Validators/checkers deterministically prove the final schema is internally consistent.

Important:
- candidate_nodes.csv, candidate_edges.csv, and candidate_conditions.csv are evidence outputs only.
- They must NOT overwrite or regenerate the final schema.
- The final schema remains:
  schema/account_graph.yaml
  schema/edge_policy.yaml
  schema/source_mapping.yaml
  schema/classification_rules.yaml
  schema/business_validation_rules.yaml
  schema/account_graph.schema.json

Only add/update files inside consumption_inventory_graph/.

Before making changes, return the exact file plan.
