Stop and simplify.

Do not pretend edge labels are fully extracted from Method3.

Correct model:
- Method3 columns deterministically produce relationship evidence.
- Canonical edge labels are curated schema vocabulary.
- The script maps evidence patterns to canonical labels using an explicit mapping table.

Do not hide this as “fully derived”.
Do not claim the final schema is automatically generated.
Do not modify 03_schema/.
Do not modify original model_linkml files.

Patch only 02_candidates/derive_candidates.py and README wording.

In derive_candidates.py:
1. Create an explicit mapping table called RELATIONSHIP_RULES.
2. Each rule should have:
   - relationship_id
   - source_columns
   - target_columns
   - relationship_meaning
   - source_node
   - target_node
   - canonical_edge_label
   - label_origin = "curated_schema_vocabulary"
   - evidence_origin = "method3_columns"
   - derivation_type = "deterministic_evidence_mapping"

3. The script should check which source_columns/target_columns exist in the Method3 truth tables/source mapping.
4. Only emit a candidate relationship when at least one evidence column exists.
5. Write:
   - 02_candidates/candidate_column_relationships.csv
   - 02_candidates/candidate_edges.csv
   - 02_candidates/candidate_node_suggestions.csv if needed

Important wording:
- relationship evidence is deterministic
- edge labels are curated canonical vocabulary
- candidate layer supports/explains the schema
- candidate layer does not generate or overwrite the final schema

Update README with exactly this explanation.

Run:
cd consumption_inventory_graph
./run_all_checks.sh

Return:
1. files changed
2. whether schema files changed
3. candidate relationship count
4. PASS/FAIL summary
