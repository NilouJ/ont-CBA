Pause. I think we may have lost the Workday/BU resolution path.

Do not change files yet.

Check the final account_graph.yaml, edge_policy.yaml, final_visual_schema_plan.md, and visjs output if generated.

Verify whether the model has an explicit Workday/person-to-BU path:

DirectoryPerson -> BELONGS_TO -> BusinessUnit

Also verify whether the model has an explicit identity/person resolution path:

Account -> HAS_IDENTITY -> Identity
Identity -> RESOLVES_TO_PERSON -> DirectoryPerson

or an equivalent supported path.

Return only this table:

| path | exists_yes_no | edge_label | source_class | target_class | supported_by_method3_yes_no | evidence_columns | action_needed |

Check these paths:
1. Account -> HAS_IDENTITY -> Identity
2. Identity -> RESOLVES_TO_PERSON -> DirectoryPerson
3. DirectoryPerson -> BELONGS_TO -> BusinessUnit
4. Account -> HAS_OWNER -> DirectoryPerson
5. DirectoryPerson/owner -> BusinessUnit through Workday fields wd_team_2/wd_team_3/wd_team_4

Important:
- Do not invent unsupported AAD-to-person edges.
- If Method3 has AD/Workday evidence to connect Identity to DirectoryPerson, mark RESOLVES_TO_PERSON as add_now.
- If Method3 only has person/owner fields directly on the account row, keep Account -> DirectoryPerson and DirectoryPerson -> BusinessUnit.
- The BU edge should be DirectoryPerson -> BusinessUnit, not BusinessUnit -> Identity.
