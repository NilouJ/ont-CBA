Now review model_linkml/canonical_contract_table.md for consistency.

Check:
1. canonical account classes are only Human, NonHuman, Unknown
2. canonical account subtypes are only Human, ServiceAccount, Batch, Unknown
3. Method3 raw types are preserved only as metadata
4. no duplicate graph node classes are introduced
5. every row has source files, join keys, output columns, graph nodes/edges, and evidence fields or says "not found"
6. Unknown/Unknown is clearly marked as downstream-derived, not native Method3 output

Return only:
- issues found
- exact fixes applied
